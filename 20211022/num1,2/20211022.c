#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>

int main(void) {
	/*
	//문제 1
	int num1, num2;

	printf("1번째 정수를 입력 : ");
	scanf("%d", &num1);
	printf("1번째 정수를 입력 : ");
	scanf("%d", &num2);

	if (num1 % num2 == 0) {
		printf("배수여부 확인 : %d는 %d의 배수입니다.", num1, num2);
	}
	else {
		printf("배수여부 확인 : %d는 %d의 배수가 아닙니다.", num1, num2);
	}

	*/
	//문제 2

	/*
	int num3, num4;
	char c1;

	printf("1번째 정수를 입력 : ");
	scanf("%d", &num3);
	printf("2번째 정수를 입력 : ");
	scanf("%d", &num4);
	printf("연산자 입력 : ");
	scanf(" %c", &c1);

	switch(c1) {
		case '+':
			printf("사칙연산 결과 : %d + %d = %d", num3, num4, num3 + num4);
			break;
		case '* ':
			printf("사칙연산 결과 : %d * %d = %d", num3, num4, num3 * num4);
			break;
		case '- ':
			printf("사칙연산 결과 : %d - %d = %d", num3, num4, num3 - num4);
			break;
		case '/ ':
			printf("사칙연산 결과 : %d / %d = %d", num3, num4, num3 / num4);
			break;
		default:
			printf("사칙연산이 아닙니다\n");
		}
	return 0;
	*/



}